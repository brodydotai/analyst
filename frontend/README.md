# Atlas Frontend

## Development

1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`
3. Open `http://localhost:3000`

## Environment

- Local configuration lives in `.env.local` (ignored by git).
- Public variables must be prefixed with `NEXT_PUBLIC_`.
